target/
SESSION.md
# The guest binary (app.bin) and its text section (app.text) are build outputs,
# no longer committed: they're built reproducibly and published as GitHub release
# assets (see .github/workflows/release-artifacts.yaml). Locally they land here
# from `cargo airbender build`.
guest/dist/
# Verification keys (fri_vk.bin, snark_vk.json) are likewise release-only assets,
# regenerated from the guest binary + trusted setup at release time.
vks/*.bin
vks/*.json
testdata/era_mainnet_batches/binary/*.bin
# Benchmarking run outputs (measured datasets, fitted tables, reports). The
# training dataset behind the committed cost table lives at
# testdata/cycle_model/dataset.json instead — see docs/benchmarking.md.
artifacts/
# Python caches from the scripts/cycle_model tooling.
__pycache__/
.pytest_cache/
*.pyc
.DS_Store